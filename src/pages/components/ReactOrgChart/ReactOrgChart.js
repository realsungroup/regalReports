import React from 'react';
import { DatePicker, TimePicker } from 'antd';
import PropTypes from 'prop-types';
import moment from 'moment';

/**
 * OrgChart 包装
 */
export default class ReactOrgChart extends React.Component {
  static propTypes = {};

  static defaultProps = {};

  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return <div>ReactOrgChart</div>;
  }
}
